package command;

import java.util.LinkedHashSet;

//import java.util.LinkedList;

public abstract class Command {
	private String command;
	public Command(String command_){
		command = command_;
	}
	protected boolean checkCommand (String[] commands){
		return commands.length == 1 && commands[0].equalsIgnoreCase(command);
	}
	
	protected static LinkedHashSet<Command> getAllCommands(){
		LinkedHashSet<Command> commands = new LinkedHashSet<Command>();
	}
	
	protected abstract void run();
}
